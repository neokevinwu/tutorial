<h1>Listing People</h1>

<%= link_to 'New Person', new_person_path %>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Weight</th>
      <th>Height</th>
      <th>Color</th>
      <th colspan="3"></th>
    </tr>
  </thead>
  
  <tbody>
    <% @people.each do |person| %>  
      <tr>
        <td><%= person.name %></td>
        <td><%= person.weight %></td>
        <td><%= person.height %></td>
        <td><%= person.color %></td>
        <td><%= link_to 'Show', person_path(person) %></td>
        <td><%= link_to 'Edit', edit_person_path(person) %></td>
        <td><%= link_to 'Destroy', person_path(person), 
                method: :delete,
                data: { confirm: 'Are you sure?' } %></td>
      </tr>
      <div id="graph" style="background-color: <%= person.color %>; width: <%= person.weight %>; height: <%= person.height %>;">
        <p><%= person.name %></p>
      </div>
    <% end %>
  </tbody>
</table> 